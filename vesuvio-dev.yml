name: vesuvio-dev

channels:
  - conda-forge
  - mantid

dependencies:
  - mantidworkbench
  - numpy==2.*     # Fix issue where numpy might downgrade 
  - flake8
  - conda-wrappers
  - pre-commit
  - coveralls
  - mock
  - pytest
  - coverage
